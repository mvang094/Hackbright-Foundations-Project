const shuffle = (arr) => {
    let question;

    for (i = 0; i <arrCopy.length; i++){
        const j = Math.floor(Math.random()*arrCopy.length);
        question = arrCopy
    }

    return arrCopy[j];
}

module.exports = {
    shuffle
}